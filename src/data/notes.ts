import { Note } from "@/types/portfolio";

// Cleared: this held the template author's posts about building the template
// itself. Repopulate and add a dock entry back in folders.ts if a journal is
// wanted.
export const notes: Note[] = [];
