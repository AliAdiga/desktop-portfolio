import { Song } from "@/types/portfolio";

// Cleared: this held the template's demo playlist. Repopulate and add a dock
// entry back in folders.ts if the Music app is wanted.
export const playlist: Song[] = [];
