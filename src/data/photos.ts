import { Photo } from "@/types/portfolio";

// Each photo becomes its own desktop icon (see src/lib/workItems.ts), so this
// doubles as your photography/design work gallery. Drop images into
// public/photos/ and list them here — empty by default so no placeholder
// stock photos ship on your live site.
export const photos: Photo[] = [
  // { id: "photo-1", url: "/photos/your-photo.jpg", caption: "Project name", date: "2026-01-01" },
];
